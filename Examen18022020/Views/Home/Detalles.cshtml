@model Pelicula

<h1>Detalles</h1>

@if (Model != null)
{
    <div class="card">
        <h4>@Model.Titulo</h4>
        <img style="width:150px" src="~/images/@Model.Foto" />
        <p>@Model.Argumento</p>
    </div>
    <div>
        <a asp-action="Cesta" asp-controller="home" asp-route-id="@Model.Id">añadir</a>
    </div>
}
